Vendored from https://github.com/shadcn/improve at 03369ee6d7ca (MIT).
Upstream inactive since 2026-06-15; maintained here.

Local changes:

- trigger narrowed to explicit `/improve`, and `disable-model-invocation: true`
  added so the model cannot fire it on its own. Without the flag the prose
  narrowing is advisory only, and it collided with `improve-animations`, which
  produces the same plan-file shape for a narrower scope.
- `execute` and `--issues` disabled.
- default effort `quick`, not `standard`.
- Hard Rule 7 added: no AI traces.
- branch-naming and "generated by" markers stripped from the plan template.
- Plan git workflow prints branch and commit commands. Done criteria are commands that were run, and a shared working tree is not required to be clean.
- Subagent prompts name a finish line and the reply names what was not read. Phase 3 writes plans/findings.md. The stops are that selection, plan's questions, and branch's offer.
- Findings table has Confidence. MED and LOW name where the auditor looked. Correctness findings include a failing case or stay at most MED.
- branch lists blocks-merge findings first. reconcile leads with what needs the author and marks a plan verified only after every criterion was re-run.
- UI plans tie visual choices to existing tokens and name the patterns they will not introduce.
